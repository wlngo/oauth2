<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="top.wei.oauth2.mapper.PermissionMapper">
    <select id="selectPermissionByUserid" resultType="PermissionDTO">
        SELECT tp.*
        FROM t_user_role_relation tur
                 LEFT JOIN t_role_permission_relation trpr ON tur.role_id = trpr.role_id
                 LEFT JOIN t_permission tp ON trpr.permission_id = tp.permission_id
        WHERE tur.user_id = #{userid}
          AND tp.deleted = 0
    </select>

</mapper>
